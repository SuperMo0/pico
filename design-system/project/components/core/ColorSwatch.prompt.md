Selectable product-colorway dot. Maps the 8 Beeko pastels by name.

```jsx
<ColorSwatch color="blue" selected label="أزرق" />
<ColorSwatch color="peach" label="مشمشي" />
```

Shows an ink ring when `selected`. Accepts any CSS color too.
